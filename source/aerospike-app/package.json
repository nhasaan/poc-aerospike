{
  "name": "aerospike-app",
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "build": "rm -rf dist && swc src --strip-leading-paths -d dist",
    "build:watch": "rm -rf dist && swc src --strip-leading-paths -d dist -w",
    "start": "node dist/main.js",
    "dev": "yarn build && yarn start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "description": "aerospike app performance test",
  "dependencies": {
    "dotenv": "^16.4.7",
    "winston": "^3.17.0",
    "winston-daily-rotate-file": "^5.0.0",
    "aerospike": "^6.0.2"
  },
  "devDependencies": {
    "@swc/cli": "^0.6.0",
    "@swc/core": "^1.10.15",
    "@types/jest": "^29.5.14",
    "@types/node": "^20.10.6",
    "jest": "^29.7.0",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Nazmul Hassan <nazmul.phpdroidx@gmail.com>",
  "license": "ISC"
}
